console.log("Starting server"); const http = require("http"); const fs = require("fs"); const path = require("path"); const os = require("os"); const root = __dirname; const port = Number(process.env.PORT || 8092); const host = process.env.HOST || "0.0.0.0"; console.log(`Server on port ${port}`); const server = http.createServer((req, res) => { res.writeHead(200, {"Content-Type": "text/html"}); res.end("Hello World"); }); server.listen(port, host, () => console.log(`LISTENING on ${port}:${host}`));
